# Решения владельца

Указатель. Одна строка на решение: что решали, что решили, почему, где лежит разбор.

**Обоснование здесь не дублируется.** Оно живёт там, где родилось — в файле постановки
или в ADR, — а эта страница только ведёт. Копия разошлась бы с оригиналом, и через месяц
было бы неясно, какая версия настоящая.

Что где искать:

| Слой | На какой вопрос отвечает |
|---|---|
| эта страница | что решили и когда |
| файл постановки | что решали, какие были варианты, чего каждый стоил |
| `git log` по файлу постановки | как формулировка менялась |
| коммиты с идентификатором задачи | чем решение обернулось в коде |
| `docs/tasks/*-review-findings.md` | чем проверяли и что нашли |

Архитектурные решения — отдельный жанр и отдельное место:
[`architecture/decisions/`](architecture/decisions), 18 ADR с отвергнутыми вариантами.

**Решения владельца по NLP-конвейеру, 2026-09-19.**

- **Нормализованная (логическая) форма заметки хранится в MongoDB**, коллекция
  `nlp_artifacts`, а не в Postgres: rebuildable-артефакт с дрейфующей схемой,
  OLTP-база не раздувается. Разбор — [`tasks/NLP-4-note-logical-form-normalization.md`](tasks/NLP-4-note-logical-form-normalization.md).
- **Чанк-векторы хранятся персистентно в pgvector** (`note_chunk_embeddings`),
  текст и происхождение чанков (heading_path, char_span) — в Mongo. Даёт парное
  сходство чанков для формулы весов связей.
- **История версий логической формы хранится** на время разработки, с флагом
  выключения `nlp.history.enabled`; статусы `current/superseded`.
- **Каскад при удалении заметки:** Mongo-артефакты + чанк-эмбеддинги + doc-вектор
  удаляются обработчиком — в Postgres нет FK на них.
- **Нормализация — только локальная** (правила + экстрактивная обработка);
  внешний LLM-вызов не рассматривается. **Режим — фоновый** (асинхронное
  обогащение); inline при сохранении — опционально на будущее. Критерий
  остановки итераций подбирается эмпирически на корпусе владельца.
- **Приоритет модели — качество, не скорость**: владелец склоняется к e5-base;
  формальное решение принимается **после консультации с Claude Code** (MODEL-2).
  Возражение Devin: выбирать по precision@k eval-харнеса W-1 на текущих
  векторах, а не по визуальной выдаче — см. W-1 §«Критика и риски».
- **Целевой масштаб ~100 пользователей:** ВМ 4 vCPU / 8 ГБ (оценка ~6–8К₽/мес
  в YC), 2 NLP-воркера — запас ×7 над пиковой оценкой нагрузки (100 зам./сутки
  на юзера).
Здесь — продуктовые и политические.

---

| № | Дата | Решение | Почему | Разбор |
|---|---|---|---|---|
| 1 | 2026-09-06 | `SKIP_AUTH` остаётся, строка пользователя в базе сохраняется | тестовый контур зависит от неё через внешний ключ `notes.creator_id` | [`tasks/AUD-2-seeder-issue.md`](tasks/AUD-2-seeder-issue.md) |
| 2 | 2026-09-07 | Проверки прогоняются **локально** перед приёмкой, а не наблюдением за CI | о красном узнаём прогоном; наблюдение за GitHub — не проверка | [`tasks/CI-3-local-check-runner.md`](tasks/CI-3-local-check-runner.md) |
| 3 | 2026-09-07 | TLS-терминация для nginx — **отложено** | вне периметра AUD-5, не блокирует 1.0 | [`BACKLOG.md`](product/BACKLOG.md), TD-TLS |
| 4 | 2026-09-07 | AUD-7b: исправить замечания, включить линтинг тестов, расширить знаменатель покрытия до `src/**`, порог 70 % | знаменатель без `src/**` льстит цифре | [`tasks/AUD-7b-lint-tests-and-coverage-denominator.md`](tasks/AUD-7b-lint-tests-and-coverage-denominator.md) |
| 5 | 2026-09-07 | Скрипт очистки Docker остаётся **машинным**, область не сужаем | чистить половину Docker бессмысленно | [`tasks/CLEAN-1-docker-cleanup-honesty.md`](tasks/CLEAN-1-docker-cleanup-honesty.md) |
| 6 | 2026-09-07 | Визуальные эталоны **не строятся** на `SKIP_AUTH` | в этом режиме фильтр по владельцу снят и `isAuthenticated()` всегда истинно — эталон фиксирует состояние, которого в проде нет, и называет его публичным | [`tasks/VIS-1-split-visual-baselines.md`](tasks/VIS-1-split-visual-baselines.md) |
| 7 | 2026-09-07 | Публичный граф — **витрина сообщества**: аноним читает опубликованное и ищет по нему | витрина, за которой ничего нельзя открыть, — не витрина | [`tasks/PUB-1-anonymous-read-and-search.md`](tasks/PUB-1-anonymous-read-and-search.md), ADR-018 |
| 8 | 2026-09-07 | Выбор графа — **режим просмотра**, авторизация задаёт лишь значение по умолчанию | иначе авторизованный не может вернуться в граф сообщества | [`tasks/PUB-2-graph-view-mode.md`](tasks/PUB-2-graph-view-mode.md) |
| 9 | 2026-09-07 | Анонимный эндпоинт публичного графа переименовывается в `/api/v1/graph/public` **жёстко, без алиаса** | внешних потребителей нет; `deprecated`, который никто не удалит, — тот же дрейф | [`tasks/PUB-3-rename-graph-endpoints.md`](tasks/PUB-3-rename-graph-endpoints.md) |
| 10 | 2026-09-07 | Авторство в публичном графе **сохраняется** | граф сообщества без авторства — анонимная свалка | ADR-018 |
| 11 | 2026-09-08 | CSP: разрешить `style-src-attr 'unsafe-inline'`, `frame-ancestors 'self'` | вся цена политики — в 76 инлайновых стилях; переписывать их сейчас не окупается | [`tasks/CSP-1-content-security-policy.md`](tasks/CSP-1-content-security-policy.md) |
| 12 | 2026-09-08 | AUTO-1 (автопуш и запуск Devin по `push`) — **отменено** | передача остаётся ручной; петля без владельца не нужна | [`tasks/AUTO-1-push-and-trigger.md`](tasks/AUTO-1-push-and-trigger.md) |
| 13 | 2026-09-08 | Спецификация API опускается до **OpenAPI 3.0.3** | вшитый Swagger UI не читает 3.1; восемь строк против нового артефакта в образе | [`tasks/API-1-openapi-contract-and-handover.md`](tasks/API-1-openapi-contract-and-handover.md) |
| 14 | 2026-09-08 | Тег `latest` на Docker Hub **не заводим** | «последний» без даты не говорит, что внутри | [`tasks/DEPLOY-2-deploy-images-from-ci.md`](tasks/DEPLOY-2-deploy-images-from-ci.md) |
| 15 | 2026-09-08 | Лицо проекта (FACE-1) **отложено до 1.0** | половина текста описывает состояние, которое 1.0 изменит; вносить сейчас — вносить дважды | [`tasks/FACE-1-project-face-from-interview.md`](tasks/FACE-1-project-face-from-interview.md) |
| 16 | 2026-09-08 | **Объём ограничивается выпуском 1.0** — граница фич и дата | «постоянно тащу новые фичи, не успеваю дотестировать» | [`tasks/FACE-1-project-face-from-interview.md`](tasks/FACE-1-project-face-from-interview.md) |
| 17 | 2026-09-12 | `yake` заменить на `keybert` (MIT) с лемматизацией рус/англ — в будущем (кода не требует) | лицензия `yake` не подходит; PR #25 закрыт | [`tasks/NLP-2-yake-replace-keybert-lemmatization.md`](tasks/NLP-2-yake-replace-keybert-lemmatization.md) |
| 18 | 2026-09-14 | Устаревшие образы **не блокируют** передачу Java-разработчику | у него есть доступ к репозиторию, он собирает из исходников | [`tasks/DEPLOY-2-deploy-images-from-ci.md`](tasks/DEPLOY-2-deploy-images-from-ci.md) |
| 19 | 2026-09-14 | Публикация заметки остаётся **отдельным действием**, мёртвое поле `is_public` в редактировании убрать | за публикацией стоит инвалидация кэша публичного графа; два способа, из которых работает один, — ложь в контракте | [`tasks/PUB-1-review-findings.md`](tasks/PUB-1-review-findings.md) |
| 20 | 2026-09-14 | Массовой публикации через граф **не заводим** | галочка в заметке закрывает потребность | [`tasks/PUB-1-review-findings.md`](tasks/PUB-1-review-findings.md) |
| 21 | 2026-09-14 | Авторство коммитов — **вариант Б**: чужая работа коммитится подписью настоящего автора | трейлер `Co-Authored-By` говорит «участвовал», а не «написал»; за месяц правило нарушено 133 раза | [`tasks/AUTHOR-1-commit-authorship-guard.md`](tasks/AUTHOR-1-commit-authorship-guard.md) |
| 22 | 2026-09-14 | Решения владельца получают **указатель** (эта страница) и сторожа; переписка остаётся в git, решения — в проекте | ценно только то, что можно найти и показать | [`tasks/DECISIONS-1-decision-index-and-guard.md`](tasks/DECISIONS-1-decision-index-and-guard.md) |
| 23 | 2026-09-07 | `BOARD-1`: доска и решения владельца подчиняются трёхдневной ретенции; решения уходят в `DECISIONS.md`; архив строк не заводится | иначе доска разрастается копиями, а решения теряются в prose | [`tasks/BOARD-1-handoff-retention.md`](tasks/BOARD-1-handoff-retention.md) |
| 24 | 2026-09-12 | `DEPENDABOT-25`: PR #25 (`yake` 0.7.3) **не мержится**; `yake` остаётся 0.4.8; выбран путь замены на `keybert` (MIT) с лемматизацией | лицензия 0.7.3 фактически AGPL; риск copyleft | [`tasks/DEPENDABOT-25-yake-license.md`](tasks/DEPENDABOT-25-yake-license.md), [`tasks/NLP-2-yake-replace-keybert-lemmatization.md`](tasks/NLP-2-yake-replace-keybert-lemmatization.md) |
| 25 | 2026-09-12 | `DEPENDABOT-79`: PR #79 (`nltk` 3.10.3) **смержен**; в `security.yml` добавлено `allow-ghsas: GHSA-8mgp-746c-j5xp` | уязвимые model-artifact API не вызываются в нашем коде; остаточный риск принят и задокументирован | [`tasks/DEPENDABOT-79-nltk-vulnerability.md`](tasks/DEPENDABOT-79-nltk-vulnerability.md) |
| 26 | 2026-09-13 | `NOTE-QUALITY-1`: цикл проверки/нормализации/обогащения заметки — без cron, пользователь управляет, дубликаты не удаляем, сети не ходим | критерии качества ещё не определены (кода не требует) | [`tasks/NOTE-QUALITY-1-quality-loop.md`](tasks/NOTE-QUALITY-1-quality-loop.md) |
| 27 | 2026-09-13 | `NOTE-TYPE-TAXONOMY`: единый порядок типов заметок — `scaleRank`, `blackhole` выше `star`, `moon` в `UI_TYPES`, дефолт `star` | иначе backend, frontend и OpenAPI держат разные списки | [`tasks/NOTE-TYPE-TAXONOMY.md`](tasks/NOTE-TYPE-TAXONOMY.md) |
| 28 | 2026-09-05 | `P11-1`: кластеризация — гибридная метрика, стабильный `cluster_id`, нормализация ключевых слов при извлечении, мультиязычная модель первой | спека `architecture/clustering.md` отложена: нет серверной геометрии (кода не требует) | [`tasks/P11-1-clustering-design-notes.md`](tasks/P11-1-clustering-design-notes.md) |
| 29 | 2026-09-12 | `SECURITY-1`: настройки GitHub/Dependabot и порядок фикса security-алертов — убрать ignore-major, Argon2 для API-ключей, принять риск #50, grouped updates | алерты и настройки должны обрабатываться явно | [`tasks/SECURITY-1-handoff.md`](tasks/SECURITY-1-handoff.md) |
| 30 | 2026-09-14 | Три открытых Dependabot-алерта закрыты как **принятый риск** (`tolerable_risk`), а не исправлены | ни один не достижим в нашем коде и ни один не чинится обновлением: у `nltk` патча не существует (`first_patched_version: null`), `cookie` зажат в `@sveltejs/kit ^0.6.0`. Открытые алерты без действия — шум, в котором утонет следующая настоящая находка | [`tasks/GITHUB-SECURITY-1-triage.md`](tasks/GITHUB-SECURITY-1-triage.md) |
| 31 | 2026-09-14 | Облачный бэкап Personal-стека — **через синхронизируемую папку, а не через API Яндекс.Диска**: файл кладётся в `Desktop\my items`, она уезжает в облако клиент владельца | OAuth-токен оказался единственным, чего не хватало для «облачного бэкапа», при том что папка и так синхронизируется. Убирается зависимость от токена и вся его ротация | [`tasks/BACKUP-2-backup-location-single-source.md`](tasks/BACKUP-2-backup-location-single-source.md) |
| 32 | 2026-09-14 | Утёкший `BACKUP_YANDEX_TOKEN` **ротирован владельцем в приложении**; в репозитории значения нет — только плейсхолдеры, `.env` в дереве отсутствует | ротация закрывает утечку у источника; после решения 31 токен бэкапу вообще не нужен | [`tasks/BACKUP-2-backup-location-single-source.md`](tasks/BACKUP-2-backup-location-single-source.md) |
| 33 | 2026-09-14 | Корневой мусор Playwright — **удалять**, не игнорировать | игнор спрятал бы причину: стандартный раннер пишет в `frontend/test-results/`, который и так игнорируется, а корневые каталоги оставил разовый ручной запуск из корня | [`tasks/BOARD-1-review-findings.md`](tasks/BOARD-1-review-findings.md) |
| 34 | 2026-09-14 | Фаза намеренно падающих тестов — **обязательна** для новых поверхностей, но сами тесты **не помечаются** ни именем, ни маркером; записывается не намерение, а то, что фаза нашла | тест ценен тем, краснеет ли он на сломанном коде, а не намерением автора; маркер в имени перестают ставить, и он начинает врать. Список находок в файле задачи проверяем, маркер — нет | [`tasks/BATCH-TEST-1-strategy.md`](tasks/BATCH-TEST-1-strategy.md), `.windsurfrules` |
| 35 | 2026-09-14 | Логирование backend — **взять `rs/zerolog`, а не писать движок заново**; прямую зависимость не размазывать по `domain` и `application` | своя реализация логгера — это поддержка того, что уже написано и оттестировано другими; изоляция за `internal/infrastructure/logger` оставляет возможность замены | [`tasks/LOG-1-zerolog-integration.md`](tasks/LOG-1-zerolog-integration.md) |
| 36 | 2026-09-14 | Порог размера доски поднят **с 40 до 120 КБ** — решение владельца, который посмотрел вычищенную доску и назвал «три раза больше, чем сейчас» | BOARD-1 запрещал поднимать порог, чтобы прогон прошёл, и запрет остаётся: менять число вправе только владелец и только записью. Основание — не прогон, а осмотр: при 39 КБ запас в 1 КБ делал доску нередактируемой без чистки в тот же заход | [`tasks/BOARD-1-handoff-retention.md`](tasks/BOARD-1-handoff-retention.md), `scripts/testing/check-board-size.mjs` |
| 37 | 2026-09-14 | Разделение слоёв доски и постановки закреплено: **на доске — краткое описание и ссылка, в `docs/tasks/` — сама постановка** | доска читается на старте каждой сессии и должна отвечать «что это и чья очередь» за один взгляд; подробности, варианты и цена каждого живут в файле, где их можно править не ломая доску. Сегодняшний LOG-1 держал постановку на 1 636 байт в ячейке таблицы — так быть не должно | [`tasks/BOARD-1-review-findings.md`](tasks/BOARD-1-review-findings.md) |
| 38 | 2026-09-15 | **Покрытие unit-тестов — 70% как цель, так и enforced min** для frontend и backend. Backend измеряется по unit-testable пакетам; CLI main, generated gRPC client, test helpers и `scripts` исключены из знаменателя, потому что не покрываются unit-тестами | порог 64.8% был переходным и расходился с target 70% во всех документах; единый min 70% убирает двусмысленность. Текущее измерение: backend 72.2% (filtered), frontend 82.2% statements / 70.7% branches | [`tasks/COVERAGE-1-align-backend-coverage-threshold.md`](tasks/COVERAGE-1-align-backend-coverage-threshold.md), `scripts/testing/backend-coverage-total.py` |
| 39 | 2026-09-16 | Замена `yake` на `keybert` — **делать сейчас, до 1.0**, а не «в будущем» (уточняет решение 17) | лицензия `yake 0.4.8` проверена по файлу внутри пакета: GPL версии 3 или новее, с коммерческой альтернативой, а не LGPL, как говорят его же метаданные. Проект MIT и распространяет образы с `yake` внутри — копилефт уже действует, оставаться на 0.4.8 ничего не решает | [`tasks/NLP-2-yake-replace-keybert-lemmatization.md`](tasks/NLP-2-yake-replace-keybert-lemmatization.md) |
| 40 | 2026-09-17 | Формулу рекомендаций **переделать**: одна реализация, три компонента, предрасчёт и живой путь дают один порядок | сейчас две неполные реализации — воркер считает граф и слова со смыслом-нулём («пока не реализован»), graph-service считает граф и смысл без слов; пользователь видит первую. Три коэффициента нигде не работают вместе, и вложения в модель до β-компонента не доходят | [`tasks/RECO-1-recommendation-formula.md`](tasks/RECO-1-recommendation-formula.md) |
| 41 | 2026-09-17 | Замер моделей эмбеддингов — **до** любой смены модели; решение по замеру принимает владелец по выдачам, не по сводному баллу | следствие решения 5: пересчёт ключевых слов (NLP-2) и автосвязи (RECO-1) зависят от модели, менять её после них — считать дважды. Текущее окно 128 токенов проверено по файлам модели, сеть умеет 512 | [`tasks/MODEL-1-embedding-model-measurement.md`](tasks/MODEL-1-embedding-model-measurement.md) |
| 42 | 2026-09-17 | Мера качества заметки — **контракт с Java-сервисом**: по ней `source-text-handler` решает, создавать заметку или вернуть сырые данные через очередь на дообработку. Отсутствие связей и ключевых слов — сигналы качества, проверяются независимо от автосвязей | одна мера закрывает три открытых вопроса разом: критерии качества, как Java создаёт заметки, как он связывает заметки без UUID. Веса меры не разработаны — это общий блокер | [`tasks/NOTE-QUALITY-1-quality-loop.md`](tasks/NOTE-QUALITY-1-quality-loop.md) |
| 43 | 2026-09-17 | Автосвязи — **подключить**: генератор готов по коду | «только подключить» оказалось четырьмя вещами, и одна — дыра в замысле: у поиска похожих нет нижней границы близости, каждая заметка получила бы две связи с любыми соседями. Порог вводится настройкой и калибруется на сиде; после смены модели связи пересчитываются | [`tasks/LINKS-1-wire-gamma-links.md`](tasks/LINKS-1-wire-gamma-links.md) |
| 44 | 2026-09-21 | Хук авторства **активен в каждом клоне** — активация не ручной шаг владельца, а свойство обвязки: клон с молчащим хуком отказывает в старте сессии | измерено 2026-09-21: `core.hooksPath` не задан ни в одном из трёх клонов, `pre-push` из AUTHOR-1 ни разу не выполнялся; два из четырёх нарушений авторства — коммиты владельца из клона, где хук молчит | [`tasks/AUTHOR-2-hook-activation.md`](tasks/AUTHOR-2-hook-activation.md) |
| 45 | 2026-09-21 | Токен Docker Hub (DEPLOY-2) — **когда будет время**, не критично; задача остаётся заблокированной без срока | образы из CI не публикуются с 17.09, но ими никто не пользуется: стенды собираются локально | [`tasks/DEPLOY-2-review-findings.md`](tasks/DEPLOY-2-review-findings.md) |
| 46 | 2026-09-21 | Личные идентификаторы в истории `ai-agents` — **принять как есть**, историю не переписывать | «лежат и лежат»: переписывание меняет хеши после 13.09 у обоих агентов, а из файлов идентификаторы уже вырезаны | [`tasks/URL-HEADING-1-review-findings.md`](tasks/URL-HEADING-1-review-findings.md) |
| 47 | 2026-09-21 | `work-nlp2/`: **заголовки заметок убрать**, таблицы чисел и колонки ключевых слов оставить | «зачем они нужны, если мы уже прогнали» — замер сделан, заголовки — личные данные владельца в публичном репозитории | [`tasks/NLP-2-review-findings.md`](tasks/NLP-2-review-findings.md) |
| 48 | 2026-09-21 | Лимиты доски — **нужны, приоритет высший**: `в работе` ≤ 3 у исполнителя, `на ревью` ≤ 5 по доске, остальное — в разделе «Бэклог» на самой доске, словарь статусов закрыт | «это важно крайне»; доска без лимитов не отвечает на вопрос «что сейчас делается»: 21.09 в разделе Devin 38 строк и ни одной `в работе` | [`tasks/BOARD-2-board-size-and-backlog-policy.md`](tasks/BOARD-2-board-size-and-backlog-policy.md) |
| 49 | 2026-09-21 | Бэкап Personal-стека — **автоматический**: событийный путь воркера пишет в синхронизируемую папку, страховка на старте стека | cron в контейнере срабатывает только при поднятом стеке в 02:00 — с 16.09 по 21.09 файлов не было; событийный путь работал и писал в слой контейнера | [`tasks/BACKUP-3-automatic-backups.md`](tasks/BACKUP-3-automatic-backups.md) |
| 50 | 2026-09-21 | URL-HEADING-1 — **решение по постановке целиком**, а не по трём вопросам порознь; постановка пишется сейчас, от MODEL-1 зависит только калибровка этапа B | «надо смотреть полностью всю задачу» | [`tasks/URL-HEADING-1-heading-extraction.md`](tasks/URL-HEADING-1-heading-extraction.md) |
| 51 | 2026-09-21 | Оценка миграции на чанкованные эмбеддинги — **сейчас дельта-разметка** (~25 сменившихся пар «было→стало» размечает владелец), после готовности контура качества — **полный размеченный корпус** с подготовкой корректных данных | «вариант один можно использовать сейчас, но при повторном прогоне нужен полный размеченный корпус» — без эталона «соседи изменились» не отвечает на «стали ли лучше» | [`tasks/CHUNK-1-measurement-findings.md`](tasks/CHUNK-1-measurement-findings.md) |
| 52 | 2026-09-22 | **DOC-REORG-1 — делать** по кластерам и иерархии из разбора DOC-AUDIT-1; канонический каталог бэкапов — **`~/Desktop/my items`** | «Да, делай reorg. Я за. Главное ничего не потерять, не сломать. Канонический каталог — ~/Desktop/my items»; исполнение Devin по явному слову владельца, постановкой служит разбор | [`tasks/DOC-AUDIT-1-review-findings.md`](tasks/DOC-AUDIT-1-review-findings.md) |
| 53 | 2026-09-22 | Ручная связь поверх автоматической — **повышение с сохранением происхождения** (одна строка на пару, `source_type` → `user`, прежний вес модели остаётся в `metadata.gamma`), а не вторая параллельная строка; плюс третье состояние пары — **«не связаны»**, которое записывается при удалении автосвязи и которое генератор обязан уважать | «сохранение происхождения мне нравится больше»; параллельные строки дали бы две линии по одной геометрии, неудаляемую связь и лишние обходы в `note_links_closure`. Без третьего состояния отвергнутая связь возвращается на каждом пересчёте | [`tasks/LINKS-2-manual-link-over-gamma.md`](tasks/LINKS-2-manual-link-over-gamma.md) |
| 54 | 2026-09-23 | Модель эмбеддингов выбирается **после дозамера MODEL-1B**, между e5-small и e5-base, по правилу, оговорённому заранее: e5-base — только если она точнее на папках за пределами шума (95-процентный бутстрэп-интервал разности p@3 выше нуля), иначе e5-small; решает владелец по выдачам (решение 41). Любая смена модели включает перекалибровку порога автосвязей под её шкалу | по выдаче поиска MODEL-1 модели примерно равны, e5-base в 9 раз медленнее и требует миграции колонки 384 → 768; таблица пар и точность на папках для кандидатов не мерились; у e5 близости ~0.9, порог 0.6 там ничего не отсекает | [`tasks/MODEL-1B-measurement-gaps.md`](tasks/MODEL-1B-measurement-gaps.md) |
| 55 | 2026-09-23 | Чанкинг с заголовком заметки в каждом чанке — **в v1** (CHUNK-1). Переход на чанкованные и нормализованные векторы делается **один раз, вместе со сменой модели** (MODEL-2), одним пересчётом, с проверкой дельта-разметкой владельца (решение 51); до того чанкер и нормализация работают за выключателями | у текущей модели ближайших соседей 82 % заметок определяют первые 128 токенов; включать три изменения по очереди — пересчитывать базу трижды | [`tasks/CHUNK-1-structure-aware-chunker.md`](tasks/CHUNK-1-structure-aware-chunker.md) |
| 56 | 2026-09-23 | Заголовки заметок владельца в файлах MODEL-1 **убираются из репозитория после выбора модели**; новые замеры кладут таблицы с названиями заметок в локальный каталог вне git, в репозиторий — только числа | принцип решения 47: пока владелец выбирает по выдачам, названия нужны ему; после — это личные данные в публичном репозитории | [`tasks/MODEL-1-embedding-model-measurement.md`](tasks/MODEL-1-embedding-model-measurement.md) |
| 57 | 2026-09-24 | **Всё тяжёлое — на D:** кэши сборок и инструментов (Go, npm, pip, Playwright, puppeteer, torch, HuggingFace), общий кэш моделей `D:\kg-hf-cache`, данные агентов (сессии Devin CLI), VM Cowork, дистрибутивы WSL; модель в образ NLP **не запекается** | на C: 2026-09-23 оставалось 7,1 ГБ: сборки и сессии агентов пишут туда по умолчанию незаметно; запечённая модель дублирует общий кэш и качается заново при каждой пересборке | [`tasks/DISK-1-everything-on-d.md`](tasks/DISK-1-everything-on-d.md) |
| 58 | 2026-09-24 | Модель **сейчас не меняется**: порядок — CHUNK-1 и NLP-4 строятся за выключателями, включение всего разом — в MODEL-2 (решение 55). Окончательный выбор e5-small/e5-base — **после повторного прогона обоих тестов** на готовом конвейере: точность p@k по папкам (положительный сценарий) и сепарация AUC (отрицательный). Рабочий кандидат на текущих данных — e5-small; публичный корпус показал сценарий, где выигрывает base, поэтому вопрос переоткрывается на прод-конвейере, а не закрывается сейчас | MODEL-1B дал разнонаправленные советы правила на двух корпусах; на заметках владельца base точнее на верхушке, но размывает границы (AUC ниже во всех парах), а менять модель до готового чанкера/нормализации — пересчитывать базу дважды | [`tasks/MODEL-1B-separation-findings.md`](tasks/MODEL-1B-separation-findings.md), [`tasks/MODEL-1B-public-findings.md`](tasks/MODEL-1B-public-findings.md) |

---

## Ждут решения

| Что | Где |
|---|---|
| Место MongoDB: убрать, заменить или дождаться нагрузки, под которую она подходит | [`tasks/MONGO-1-drafts-storage-discussion.md`](tasks/MONGO-1-drafts-storage-discussion.md) |
| Как Java-сервис создаёт заметки и связывает их, не зная UUID | [`tasks/BATCH-1-api-design.md`](tasks/BATCH-1-api-design.md) |
| DEPENDABOT-1: судьба оставшихся Dependabot-PR | [`PROJECT_REVIEW_AI_AGENTS.md`](PROJECT_REVIEW_AI_AGENTS.md), §20 |
| Состав и дата 1.0 | — |

---

## Решения владельца

**Решение владельца по доске, 2026-09-21.**

Закрытые строки доски (принято/отклонено/отменено) переносятся в раздел
«Архив» сразу при закрытии, без трёхдневной ретенции в активных разделах.

**Решение владельца по work-nlp4/notes_dataset.json, 2026-09-21.**

108 заметок владельца с полным текстом попали в историю публичной ветки
ai-agents (коммиты 754dc0e, e5abf99, замер NLP-4). Из дерева файл убран;
историю **не переписываем** — принимаем как есть. Урок: дампы корпуса с телами
заметок не коммитятся; в git — только синтетика и агрегированные метрики.

- Удаление данных Personal-стека допускается **при наличии свежего непустого бэкапа**, а не запрещается полностью.
- Автокоммит из регрессионного цикла удалён совсем.
- `ARGOS_REFERENCE_BRANCH` переводится на `main` после починки A-1.
- Мастер-промпты остаются самодостаточными копиями; консистентность обеспечивается правилом синхронизации.
- **Рабочий язык AI-документов — русский**, английские дубликаты для них не требуются.
- **Язык приложения по умолчанию — английский (`en`)**, Russian (`ru`) поддерживается через i18n-ключи; пользователь может переключиться на Russian в UI. Авторитетная продуктовая/API/архитектурная документация — на русском по умолчанию, английские переводы могут поддерживаться параллельно.

**Решение по `SKIP_AUTH` и визуальным эталонам, 2026-09-06.**

- **`SKIP_AUTH` остаётся.** Он задуман как тестовый обход для бэкенда, и на нём держится весь набор по умолчанию: `npm test`, `test:skipauth`, `test:headed`, `test:debug`, `test:smoke`, `test:bdd:skipauth`. Выключается только в `test:realauth` и `test:bdd:realauth`. Ни удалять, ни переделывать под визуальные тесты не нужно.
- **Визуальные эталоны на `SKIP_AUTH` не строятся.** В этом режиме `applyNoteScope` (`note_repo.go:40-44`) снимает фильтр по владельцу целиком, и снимок фиксирует состояние, которого в проде не существует. Проверять эталон несуществующего состояния хуже, чем не проверять.
- **Переключает вид не `SKIP_AUTH`, а токен.** `shared/api/graph.ts:253`: `isAuthenticated() ? "v1/graph/full" : "v1/graph/public"`. Решение принимает клиентский стор, поэтому менять `SKIP_AUTH` ради смены вида бессмысленно — на стенде бейдж «SKIP AUTH» виден, а граф грузится публичный.
- **Эталонов будет два, несимметричных.** Основной — авторизованный граф под `testuser`: данных для него достаточно уже сейчас (101 узел, 60 связей), правки сидера не требуется. Второй — публичный граф анонимно: отдельная продуктовая поверхность, но ей нужен засев связанных публичных пар (сейчас 20 узлов, 0 связей).
- Порядок: сначала авторизованный, потом сидер, потом публичный, и только после починки сигнала готовности в 3D. Обоснование — [`tasks/A-1-review-findings.md`](tasks/A-1-review-findings.md).
- Находки внешнего аудита живут в [`EXTERNAL_AUDIT_2026-09.md`](archive/EXTERNAL_AUDIT_2026-09.md), рядом с `AI_PROCESS_AUDIT.md`.
- Первая партия по аудиту — изоляция данных и транспорт токена; витрина репозитория делается параллельно силами Claude Code.
- Судьба 125 осиротевших BDD-сценариев решается **по цифрам разведки**, не вслепую.
- Контур передачи перестраивается: одна команда вместо двух, режим выбирается автоматически, ревью и реализация не совмещаются в одной сессии.
- **Ретенция доски:** закрытые строки держатся ~3 дня, затем удаляются. Журнал не хранится вечно — его прошлое достаётся из истории git.
- **Кластеризация делается по заметкам, гибридной метрикой:** семантика как основа, существующие связи усиливают близость. Иерархия и zoom из спеки `architecture/clustering.md` откладываются — она опиралась на серверную геометрию графа, которой в проекте нет.
- **Порядок работ по кластеризации:** мультиязычная модель эмбеддингов → нормализация ключевых слов → сама кластеризация. Модель первой: её замена обесценивает всё, что посчитано до неё.
- **Ключевые слова нормализуются при извлечении**, а не при использовании: это чинит заодно рекомендации, которые сейчас сравнивают словоформы.
**Решения владельца по CSP, 2026-09-08.**

- **Инлайновые стили разрешаются** директивой `style-src-attr 'unsafe-inline'`, 76 мест не переписываются. Снятие уступки — записью `TD-CSP-STYLES` в [`BACKLOG.md`](product/BACKLOG.md).
- **`frame-ancestors 'self'`**, как сейчас, а не строже: замысел графа сообщества допускает будущий встраиваемый виджет публичного графа, и закрывать эту дверь политикой незачем.

**Решение владельца, 2026-09-08.** Скрипт очистки Docker остаётся **общемашинным** — машина отдана под этот проект, сужать охват до `kg-*` не требуется. Чинится не область, а честность отчёта и предохранители: код выхода, dry-run, проверка бэкапа. Постановка CLEAN-1.

**Решения владельца по публичному графу, 2026-09-07.**

Замысел, как его сформулировал владелец: публичный граф — витрина сообщества. Неавторизованный посетитель видит опубликованные заметки, читает и ищет по ним; после входа переключается на своё. Дальше — подграфы отдельных сообществ.

- **Анониму открывается чтение публичной заметки и поиск по публичному подмножеству.** Сегодня граф рисуется, но узел не несёт содержимого, а чтение и поиск отвечают `401` — замысел выполнен наполовину. Постановка PUB-1.
- **Граф сообщества становится режимом просмотра.** Авторизация задаёт умолчание, а не запирает выбор: залогиненный может вернуться к общему графу. Без этого подграфы сообществ некуда встраивать. Постановка PUB-2.
- **`/api/v1/graph/public` — имя анонимного публичного графа, прежний путь переименован жёстко, без алиаса.** Имя `all` обещало полный граф, а отдавало публичное подмножество. Совместимость не поддерживаем: внешних потребителей нет, а `deprecated`-путь, который никто не удалит, — тот же дрейф, только вежливый. Постановка PUB-3.
- **Имена авторов в публичном графе остаются** — это атрибуция, без неё граф сообщества превращается в анонимную свалку. Решение осознанное; при публикации заметки пользователя стоит предупреждать.

**Решение владельца по автономности агентов, 2026-09-07.**

- **Автопуш в конце сессии плюс запуск Devin по событию `push`.** Полную петлю по расписанию пока не заводим.
- **Отдельный канал связи — почта, вебсокет, API — не заводится.** Транспорт уже есть: git и эта доска, у них есть история, ревью и откат. Второй канал означал бы второй источник правды.
- **Граница автоматизации:** она несёт исполнение и ревью, но не решения. Петля останавливается на разделе «На человеке».

**Решения владельца, 2026-09-07.**

- **О красном CI узнаём локальным прогоном, а не наблюдением за GitHub.** Перед тем как пометить задачу принятой, агент гоняет `check-all` и прикладывает сводку. Бейдж и почтовые уведомления отвергнуты: первое пассивно, второе уходит в спам.
- **TLS откладывается.** У проекта нет домена и публичного хостинга, поэтому TLS сейчас не даёт ничего, кроме работы. Записано в бэклог как TD-TLS; периметр уже разделён средствами nginx и graph-service.
- **`SKIP_AUTH` остаётся зависимым от строки в базе.** Сидер починен, тест-стек работает, прогоны зелёные. Переделка трогала бы схему БД ради удобства, которого никто не просит.
- **Сняты с доски:** тикет по сидеру (баг починен и принят), `.github/CODEOWNERS` (файл удалён), MCP-коннекторы (настройка аккаунта, не репозитория). Каталог `.kilo/` удалён.

- **Граф двуязычный по требованию.** Решение — одна мультиязычная модель и единое векторное пространство; пара одноязычных моделей отвергнута, у них несопоставимые пространства.
