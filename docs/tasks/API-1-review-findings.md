# Ревью API-1 (Claude Code, 2026-09-13)

**Принято.** Все восемь критериев проверены исполнением, блокеров нет.

## Критерии приёмки — по одному

1. **UI показывает операции, а не плашку об ошибке.** Проверено мной в браузере на
   работающем бэкенде (образ собран 2026-09-11, содержит `b00df18`), только чтением —
   ни одной записи. JS-проба по DOM:

   ```
   ops: 72   errors: 0   title: "Knowledge Graph API 1.1.0 OAS3"   unable: false
   ```

   Скриншот Devin в `docs/assets/api-1/swagger-render.png` показывает то же.
2. **Расхождений спека ↔ роутер ноль.** `TestRouterMatchesOpenAPISpec` зелёный;
   `openapi: 3.0.3`, union-типов `type: [...]` — 0, `nullable: true` — 8.
3. **Контрактный тест ловит обе стороны** — проверено тремя мутациями, каждая
   откачена через `git checkout`:
   - маршрут в роутере без описания (`GET /api/v1/zz-mutation-probe`) →
     `in the router, not described in openAPI.yaml`;
   - удалена операция `put` у `/api/v1/links/{id}` → тот же маршрут назван, та же
     сторона;
   - фантомный путь в спеке (`GET /api/v1/zz-phantom`) →
     `described in openAPI.yaml, not in the router`.

   Имя маршрута и сторона расхождения — в каждом сообщении.
4. **Роутер в полной комплектации.** `&drafthandler.Handler{}` передан
   (`router_contract_test.go:53`), нижняя граница `>= 70` стоит (`:73`). Ловушка с
   условной регистрацией черновиков учтена.
5. **Пакет `backend/docs` убран** — каталога нет, слепого импорта в `main.go` нет,
   `COPY` в `Dockerfile` нет. `swag` из `go.mod` тоже ушёл.
6. **Живая проверка** — см. пункт 1; `GET /openapi.yaml` → 200, 76100 байт, 3.0.3.
7. **Раздел передачи** — `docs/API_EN.md` есть, проиндексирован в `docs/README.md:33`
   и в корневом `README.md:298`. Порты сверены с compose-файлами: 18083
   (`docker-compose.test.yml:94`), 9000 (`docker-compose.yml:90`), 18080 (nginx) —
   все совпадают.
8. `check-all` — см. сводку в журнале за сессию.

## Мелочь, чинить не в ущерб очереди

- Комментарий над `serviceRoutes` в `router_contract_test.go:20` говорит, что `/health`
  исключён как проба, а в самом списке `/health` нет — и правильно, что нет: он описан
  в спеке и сверяется. Комментарий отстал от кода.
- Коммит `b00df18` записан под авторством владельца, хотя работу делал Devin. Протокол
  просит сохранять настоящего автора; для истории проекта это не мелочь — ровно на
  `git log` мы смотрели, обсуждая, чей это проект. Не переписываю: чужой коммит.

## Что важнее самой задачи

Спецификация теперь правдива относительно кода в `main`. Но образы на Docker Hub, к
которым `docs/API_EN.md` ведёт второго разработчика, собраны 2026-09-08 и **не содержат**
batch-маршрутов, описанных в этой же спецификации. Подробно — в
[`DEPLOY-review-findings.md`](DEPLOY-review-findings.md), там же постановка DEPLOY-2.
