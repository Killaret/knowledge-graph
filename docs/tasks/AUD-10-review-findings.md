# Ревью контура передачи и смежных артефактов

Ревьюер: Devin. Предмет: коммиты `0f9c040` (AUD-1 + постановки) и `add5d73` (контур `/kg-work`, доска, журнал). HEAD при ревью — `add5d73`.

**Общий вердикт: принято.** Ослабления собственных ограничений Claude Code не обнаружено — прямой ответ на вопрос из постановки AUD-10. Два замечания ниже блокерами не являются.

## Проверено исполнением

| Утверждение | Проверка | Итог |
|---|---|---|
| `/kg-work` сохраняет проверку исполнением, вердикт файлом, запрет Personal-стека и чужих разделов | Сопоставление `.claude/commands/kg-work.md` с удалёнными `kg-handoff.md`/`kg-review.md` | Все четыре пункта на месте; добавлено «не менять свои собственные ограничения» — контур стал строже, не мягче |
| Гейт режимов однозначен | «Строки `на ревью`, автор которых — X»; на этой доске авторство читается из секции, где висит строка. Живой прогон — сама эта сессия: на доске были строки `на ревью` от Claude Code, и режим выбран ревью | Работает |
| Запрет самопроверки при пустой очереди | «Свои строки в статусе `на ревью` пропускай… даже когда очередь пуста» | Закрыт |
| Формат доски | Колонки `Что/Где/Статус/Обновлено` во всех трёх таблицах, статусы и ретенция в шапке | Соответствует |
| Журнал | `docs/AI_LOG.md` существует, только дописывается, ретенция заявлена в шапке | Соответствует |
| P-1: путь хука через `git rev-parse` | Из `backend/` подан JSON-хук: `docker volume prune -f` → `permissionDecision: deny`; `cd backend && go test` → молчание, exit 0 | Подтверждено |
| `/kg-handoff` и `/kg-review` вне дерева `.devin/` | `rg` по репозиторию: остались только в исторических репликах доски, файлах находок и постановке AUD-10 | Чисто; сторона `.devin/` — моя очередь |
| AUD-1: `LICENSE` MIT, `go 1.25.0`, порт `19091`, снос авто-коммита из `TESTING.md`, числа `tests/README.md` | `go.mod`; `docker-compose.test.yml:129-130`; подсчёт: 120 Go-тест-файлов, 107 спек Vitest, 24 спека Playwright, 13 фич/125 сценариев в `tests/features`, 2 фичи/9 сценариев в `frontend/tests/features` | Всё сходится |
| Постановка AUD-2 по коду | `019` up/down: `...0000` vs `...0001`; `APP_ENV`/`AppEnv`/`GIN_MODE` отсутствуют; `note_repo.go:41` обход фильтра; `router.go:79-81` безусловный `public`, применён на 159/166-168/177-180; последняя миграция 028 | Всё сходится |
| Постановка AUD-3 по коду | `extractToken` принимает `?token=` и сырой заголовок, ошибки не возвращает; `config.go:458` дефолт PKCE `true`; `handler.go:635` `plain`; `state` хранится и проверяется только в ветке PKCE (612-620, 654) | Всё сходится |
| Постановка AUD-6 по коду | `cucumber.mjs` смотрит только в `frontend/tests/features`; CI-шаг `_core-checks.yml:216-217` типизирует осиротевший набор; `tests/features/support/world.ts` со своим World есть | Всё сходится |
| T-2/T-4 из аудита | `_core-checks.yml:50` — `go test` без `-coverprofile`; `frontend-tests.yml:52` — `test:coverage`; `playwright.config.ts:61-64` — `bypassCSP` и `--disable-web-security` | Подтверждено |

## Замечания (не блокеры)

**1. Методология ревью урезана.** Из `kg-review.md` в `kg-work.md` не перешли четыре требования: «смотри дифф целиком, а не сводку автора», «проверь, что способ проверки автора вообще способен поймать дефект» (ровно тот приём, которым ловился A-3 в раунде 3), «раздели на блокеры и мелочь», «отметь принятое без замечаний». Это не ослабление ограничений — ограничения усилены, — но это ослабление качества ревью, и чек-лист стоит вернуть хотя бы в сокращении. Формулировка согласована с человеком, поэтому считаю это пожеланием, а не дефектом контура.

**2. Журнал и `git log` расходятся по авторству.** В `AI_LOG.md` строки `b4c1b88` (закрытие обхода `bash -c`, 36 проверок guard), `37d3157` (префикс `kg-`) и `4f0f515` (путь хука) записаны за Devin, но все три коммита авторства `Claude Opus 5`. Протокол требует: «`git log --author` должен отвечать на вопрос, кто что писал» — сейчас журнал и git противоречат друг другу. Либо колонка «Агент» означает «чья работа» и тогда атрибуция неверна, либо при коммите чужой работы авторство не сохранено. Нужно одно из двух: поправить журнал или зафиксировать семантику колонки.

**3. Косметика в постановке AUD-6.** «`tests/features/step_definitions/` (6 файлов)» — фактически 7. На содержание задачи не влияет.

## Наблюдения вне предмета ревью

- `run-full-test-cycle.ps1:463` — устаревший комментарий «Cleanup moved to after state checks / auto-commit» остался в файле (мой, поправлю в своей очереди).
- `.windsurfrules` таблица Testing говорит о минимуме 60%/55% для фронтенда, тогда как `vitest.config.ts:96-101` применяет 70 по всем метрикам. README теперь ссылается на `.windsurfrules` как единственный источник — источник сам чуть отстаёт от конфига. Мелочь, помечаю, чтобы не потерялась.
- Бэкап Personal-стека старше 24 ч — деструктивные команды сейчас корректно блокируются.
