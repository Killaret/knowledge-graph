# DOC-AUDIT-1 — разбор ревью

Ревьюер: Claude Code. Дата: 2026-09-22. Реализация: Devin, `2efbfd3`, `e430b2a` (2026-09-21),
по прямому запросу владельца. Файл задачи: [`DOC-AUDIT-1-documentation-inspection.md`](DOC-AUDIT-1-documentation-inspection.md).

**Вердикт: принято.** Девять исправлений на месте и верны, ни одно не выдумало факта;
список «нашёл, не трогал» и предложение по иерархии — материал для решения владельца, не для
приёмки.

## Проверено

| Исправление | Вердикт |
|---|---|
| `YANDEX_DISK_BACKUP.md` — баннер устаревания с решением 31 | есть, ссылка на `BACKUP.md` |
| `CLOUD_BACKUP_SETUP.md` — указатель на `BACKUP.md` | есть; в индексе помечен архивным (Cloudflare R2) |
| `AUTO_LINK_CREATION_PLAN.md` — статус «частично реализовано LINKS-1» | есть, со ссылками на постановку и находки; честно: keyword/граф-компоненты остаются планом |
| `MANUAL_TEST_CHECKLISTS_RU.md` — указатель на актуальный чек-лист | есть, архивный назван архивным |
| `LICENSES.md` в индексе | есть |
| `gordon/` и `TZ-Java-…` объяснены в индексе | есть |
| нюанс `getJSONFloatOrDefault` в EN и RU | есть в обоих; проверен по коду: `config.go:570` действительно отдаёт значение из JSON всегда, когда файл загружен — отсутствующий ключ = 0 |
| `ARCHITECTURE_SUMMARY.md` — откуда 0.6/степень 2 | есть (`:220`), внесено коммитом LINKS-1 `30f4ce3`, не этими двумя; ссылка на W-1 проверена: `W-1-eval-findings.md` §3 действительно рекомендует 0.55–0.6 и степень 2–3 |
| `API_ERRORS_EN.md` — лишние байты «да» перед заголовком | убраны, файл начинается с `# A` |

`check-docs-links`, `check-tasks-index` — зелёные; `check-all` 2026-09-22: 24 PASS, 0 FAIL,
3 SKIP (golangci-lint нет; интеграция — Docker недоступен).

## Наблюдения — не блокеры

1. Нюанс конфигурации записан как **правило** («новые ключи добавлять в `config/*.json`»), а не
   как дефект. Для порога автосвязей это дефект — см. блокер 2 в
   [`LINKS-1-review-findings.md`](LINKS-1-review-findings.md); класс целиком — строка бэклога
   CONFIG-1. Когда починят, абзац в `CONFIGURATION_*` надо будет переписать.
2. Процессная находка про NLP-4 верна: постановка Devin без ревью Claude Code. Она уже в моей
   очереди бэклога, реализацию до ревью не начинать — записано в строке NLP-4.
3. Предложение DOC-REORG-1 (кластеры дублирования, иерархия `docs/`) — решение владельца:
   переезд рвёт относительные ссылки в 130 постановках, это не «поправить по дороге».
   Отдельной строкой бэклога «ждёт владельца».
4. Коммит `2efbfd3` перевёл доску в CRLF и обратно (233 строки диффа ради двух) — тот класс,
   который закрыл `.gitattributes` из BOARD-2 через два часа.

## Принято без замечаний

- Метод назван: шапки 60 документов + машинная сверка индекса; что читалось, а что сверялось
  с кодом — разделено.
- Ничего не удалено и не перемещено без решения — только баннеры, указатели, индекс.
