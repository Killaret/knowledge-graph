# DOC-REORG-1 — разбор ревью

Ревьюер: Claude Code. Дата: 2026-09-23. Реализация: Devin, `15809ec`, `1bdcd47` (2026-09-22),
по решению владельца 52 («делай reorg, главное ничего не потерять, не сломать»). Заметки
исполнителя: [`DOC-REORG-1-implementation-notes.md`](DOC-REORG-1-implementation-notes.md).

**Вердикт: принято.** Оба условия владельца проверены машинно, а не выборочно: ни один файл
не потерян, ни одна ссылка не сломана — ни в документах, ни в скриптах и конфигурациях.

## «Ничего не потерять»

`git show 15809ec -M --name-status` — классификация всех изменений:

| Вид | Сколько |
|---|---|
| переименования (перемещения) | **89**: 50 без изменения содержимого, остальные со сходством 81–99 % |
| чистые удаления | **0** |
| изменённые на месте | 84 (переписанные ссылки) |
| добавленные | 2 — заметки исполнителя и скрипт миграции |

Для каждого перемещения с изменённым содержимым сравнил старую и новую версию, вырезав цели
ссылок и пути `…md`: в 38 файлах из 40 отличий, кроме путей, **нет**; в двух
(`operations/TESTING.md`, `operations/TESTING_COMMANDS.md`) добавлены взаимные ссылки — по
разбору DOC-AUDIT-1, намеренно.

Единственное сокращение содержимого — `docs/AGENTS_EN.md`: 91 строка заменена указателем на
`AGENTS.md`. Это предложение самого разбора DOC-AUDIT-1, одобренного владельцем целиком.
Проверил, что уходит: роли «Windsurf SWE 1.7 Max» и «DeepSeek» (неактуальные) и тестовые
команды, которые есть в `COMMANDS.md` и `operations/TESTING.md`. Уникального живого содержимого
не потеряно; прежний текст — в истории git, указатель об этом говорит.

## «Ничего не сломать»

- `check-docs-links` — зелёный: все локальные ссылки в `.md` разрешаются.
- **Пути в скриптах, конфигурациях и workflow** — сторож ссылок их не видит, поэтому проверил
  отдельно: все литералы `docs/…` во всех отслеживаемых файлах, кроме `.md`, — **206**, каждый
  разрешён в существующий файл. Не разрешаются только пути в таблице переездов самого
  `scripts/devops/migrate-docs-reorg.mjs` — там они старые по назначению.
- Уцелевшие упоминания старых путей по всему дереву — два, оба законные: историческая
  пометка «бывший `docs/3d-archive/`» в `archive/README.md` и перечни файлов в старых строках
  `AI_LOG.md` — журнал не переписывается.
- `check-all` 2026-09-23 без `-Quick` — сводка в журнале.

## Мелочи

1. Скрипт миграции оставлен в `scripts/devops/` «как запись трансформации». Как запись он
   полезен, но повторный запуск на новом дереве переписал бы уже переписанные пути по той же
   таблице — стоит одной строкой в начале файла сказать, что он одноразовый и не
   идемпотентен, или вынести в `docs/archive/`.
2. Отклонения от предложенной иерархии (протокольные файлы в корне `docs/`, `AGENTS.md` на
   месте, `TESTING_COMMANDS.md` не слит, чек-листы не слиты) перечислены исполнителем с
   причинами — принимаю все; слияние командных документов и чек-листов остаётся редакционной
   задачей на будущее, не частью переезда.

## Принято без замечаний

- Перемещения через `git mv` — история каждого файла прослеживается `git log --follow`.
- Литералы в производных (`.windsurfrules`, `CLAUDE.md`, промпты, скиллы, `kg-work`) обновлены
  вместе с переездом — правило «изменил норму — обнови производные» соблюдено.
- Сторож авторства научен не считать перепись ссылки новым заявлением (`061daef`) — без этого
  переезд сделал бы исполнителя «автором» чужих строк журнала.
