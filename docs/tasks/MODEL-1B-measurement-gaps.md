# MODEL-1B. Дозамер моделей: точность на папках, порог, полный конвейер

Постановка: Claude Code, 2026-09-23. Исполнитель: Devin. **Первая в очереди блока моделей.**
Продолжение [`MODEL-1`](MODEL-1-embedding-model-measurement.md): её замер закрыл скорость, память и
выдачу поиска, но оставил три дыры, без которых выбор модели делать рано.

**Решение владельца (2026-09-23, MODEL-1B):** перед выбором модели — этот дозамер. Выбор — между
`multilingual-e5-small` и `multilingual-e5-base` по заранее оговорённому правилу (ниже); решение
остаётся за владельцем и принимается по выдачам (решение 41), точность — дополнительная линза.
Любая смена модели включает перекалибровку порога автосвязей под шкалу новой модели.

## Три дыры MODEL-1

1. **Таблица близости пар есть только для текущей модели.** В `MODEL-1-review-findings.md` под
   каждым кандидатом вместо пар стоит строка параметров (ошибка сборки отчёта), в
   `MODEL-1-findings-raw.md` таблица 2 есть только для `current@128`. А это то, из чего
   строятся автосвязи.
2. **Точность по папкам мерили только для текущей модели.** Харнес W-1
   (`nlp-service/scripts/eval_link_formula.py`) умеет подменять векторы, но кандидатов через
   него не прогоняли. Для `current@128`: p@1 = 0.506, p@3 = 0.451 (exact).
3. **У e5 другая шкала близости.** У текущей модели первый сосед на корпусе владельца в среднем
   0.62 — порог `GAMMA_LINK_MIN_SCORE = 0.6` проходит примерно половина заметок. У e5-base
   ближайшие соседи 0.88–0.92 (`CHUNK-1-measurement-findings.md`) — с порогом 0.6 каждая заметка
   связалась бы с двумя ближайшими, какими бы они ни были. Порог надо выводить заново для
   каждой модели.

Плюс подозрение: e5-base в MODEL-1 медленнее e5-small в 9 раз (2 320 против 256 мс), а по объёму
вычислений ожидается раза в четыре. Перемерить с одинаковыми настройками потоков.

## Варианты

Восемь, на одном корпусе, одним скриптом:

| | Модель | Текст | Вектор |
|---|---|---|---|
| A | текущая, окно 128 | сырой | усечение — **как сейчас в проде** |
| B | e5-small, окно 512 | сырой | усечение |
| C | e5-base, окно 512 | сырой | усечение |
| D | текущая, окно 128 | нормализованный | чанки, среднее, заголовок в каждом чанке |
| E | e5-small | нормализованный | чанки, среднее, заголовок в каждом чанке |
| E′ | e5-small | нормализованный | чанки, среднее, **без** заголовка |
| F | e5-base | нормализованный | чанки, среднее, заголовок в каждом чанке |
| F′ | e5-base | нормализованный | чанки, среднее, **без** заголовка |

D–F — то, что пойдёт в прод после MODEL-2: нормализация (NLP-4, один проход с предохранителями) и
чанкер (CHUNK-1, среднее по чанкам, заголовок в каждом чанке — решение владельца). Здесь их
прототипы из `measure_normalization.py` и `measure_chunking.py` — промышленных модулей ещё нет,
расхождение с ними оговорить в находках. Префиксы e5: `passage:` для заметок, `query:` для
запросов поиска.

## Что мерить

1. **Точность по папкам** — харнес W-1 без изменений метода: p@1, p@3, p@5 на трёх
   гранулярностях (exact, l1, parent), 79 заметок с непустой папкой.
2. **Кривая порога** — для каждого варианта, степень 2 (как в проде): для порогов 0.30…0.95 с
   шагом 0.05 — доля заметок, получивших хотя бы одну связь, и точность этих связей (цель в той
   же папке, exact). Отдельной строкой — **t\*: наименьший порог, при котором точность ≥ 0.50**
   (ориентир W-1). Это и есть стартовое значение `GAMMA_LINK_MIN_SCORE` для модели.
3. **Распределение близости** первого и второго соседа — медиана и среднее для каждого варианта.
4. **Шум.** Бутстрэп по заметкам (1 000 повторов): 95-процентный интервал разности
   p@3 exact **F − E**. Это то, на чём стоит правило выбора.
5. **Скорость и память** — для A, E, F: время на заметку целиком (нормализация + чанки + векторы),
   p50/p95; время на один чанк; RSS. Для e5-small и e5-base — одинаковые `torch.get_num_threads()` и
   `OMP_NUM_THREADS`, значения записать. Объяснить разрыв 9× или показать, что он был артефактом.
6. **Для глаз владельца** — таблица близости пар (top-15 на тех же 20 заметках) для всех восьми
   вариантов и выдача поиска top-10 по десяти запросам MODEL-1 для A, E и F.
7. **Лист дельта-разметки** (решение 51): 25 заметок, у которых первый сосед в лидирующем варианте
   (E или F — у кого выше p@3) отличается от A. В строке: заметка, прежний сосед, новый сосед,
   колонка для отметки владельца «было лучше / стало лучше / оба мимо».

## Правило выбора — оговорено заранее

- Если 95-процентный интервал разности p@3 exact **F − E** целиком выше нуля — e5-base лучше за
  пределами шума: **e5-base**, с миграцией колонки 384 → 768 и медленной фоновой обработкой
  (владелец ставит качество выше скорости).
- Иначе — **e5-small**: та же размерность 384, колонка не меняется.
- Правило подсказывает, решает владелец: смотрит выдачи и лист разметки. Итоговая строка находок —
  что выбирает правило и на каких цифрах.

## Данные и приватность

- **Тексты заметок** — из локального дампа, который уже использовался в NLP-4 и CHUNK-1
  (`work-*/notes_dataset*.json`, в `.gitignore`); **папки** — из `work-w1/dataset.json` по
  `note_id`. Если дампа нет — новая выгрузка Personal только на чтение и **только с явного
  разрешения владельца**, как в MODEL-1.
- **В репозиторий — только числа.** Всё, где видны названия или тексты заметок (пары, выдача
  поиска, лист разметки), — в `work-model1b/local/` (каталог в `.gitignore`); в находках назвать
  абсолютный путь, чтобы владелец их открыл. Это принцип решения 47 — не добавлять личные данные
  в публичный репозиторий, пока старые убираются.

## Починить в скрипте

`nlp-service/scripts/measure_models.py`: сборка отчёта теряет таблицу 2 для всех вариантов, кроме
первого. Исправить и закрепить тестом генератора отчёта на двух поддельных моделях — обе таблицы пар
на месте.

## Критерии приёмки

1. `MODEL-1B-findings.md` (только числа): таблица точности 8 × (p@1, p@3, p@5 × exact/l1/parent);
   кривые порога и t\* для каждого варианта; распределения близости; интервал F − E; скорость,
   память, настройки потоков; итоговая строка правила.
2. `work-model1b/local/` с таблицами пар (8 вариантов), выдачей поиска (A, E, F) и листом
   разметки; путь назван в находках.
3. **Приватность проверена машинно:** скрипт сверяет отслеживаемые файлы этой задачи с заголовками
   локального корпуса — ноль совпадений; вывод приложить.
4. Тест генератора отчёта; мутация «вернуть потерю таблицы 2» — тест красный.
5. Воспроизведение одной командой — в находках.
6. Прод не тронут: `git diff` по `.env*`, compose, `config.go`, умолчаниям модели в `nlp-service/app`
   пуст.
7. `check-all` без `-Quick` — сводка в строке доски или журнале.

## Чего не делать

- Не менять модель, окно и порог в проде — это MODEL-2 после выбора.
- Не поднимать Personal без разрешения владельца.
- Не добавлять bge-m3 и rubert-tiny2 — первая слишком тяжела для CPU (MODEL-1), вторая выбыла по
  выдаче (на английских запросах все пять первых результатов мимо).

## Исполнение (Devin, 2026-09-23)

Все восемь вариантов прогнаны одним скриптом на локальном корпусе (108 заметок, 76 с непустой
папкой — оценочное множество пересечение корпуса с дампом папок). Числа — в
[`MODEL-1B-findings.md`](MODEL-1B-findings.md); таблицы с названиями, выдача и лист разметки —
в `work-model1b/local/` (в `.gitignore`, абсолютный путь назван в находках).

Критерии приёмки: 1 ✅ findings: точность 8 × 9 метрик, кривая порога 0.30–0.95 и t*,
распределения соседей, интервал F − E, скорость/память/потоки, строка правила;
2 ✅ `work-model1b/local/`: pairs.md (8 вариантов), search.md (A/E/F), delta-sheet.md (лидер F);
3 ✅ приватность — `nlp-service/scripts/check_corpus_privacy.py --changed`: 0 совпадений
заголовков в изменённых файлах; 4 ✅ тест генератора `tests/test_measure_models_report.py`
(2 теста), мутация `for n in names[:1]` в таблице 2 — оба теста красные, откачена;
5 ✅ одна команда в находках; 6 ✅ прод не тронут (`measure_*` — скрипты замера);
7 ✅ `check-all` без `-Quick` — сводка в журнале.

Починка отчёта MODEL-1: `measure_models.py` раньше при `--only` дописывал в файл отдельный
одновариантный блок — при сборке итогового документа оставалась таблица 2 только первого
варианта. Теперь результаты накапливаются в `<out>.state.json`, отчёт перегенерируется целиком
по всем накопленным вариантам (`write_report`).

Сводка: p@3 exact — A 0.465, B 0.561, C 0.553, D 0.575, E 0.548, E′ 0.553, F 0.583, F′ 0.561.
Бутстрэп F − E (p@3 exact, 1000 повторов): 95% CI [−0.0132; 0.0789] — включает ноль → по
заранее оговорённому правилу выходит **e5-small**. Разрыв скорости e5-base/e5-small при
одинаковых потоках (torch=6): ~3× по чанку (271/86 мс) и ~2.3× по заметке (395/169 мс) — девятикратный
разрыв MODEL-1 был артефактом разных условий. Находка для CHUNK-1: у варианта D (окно 128)
title-инъекция обрезает хвост у 253 чанков — заголовок надо считать в бюджете окна.
У e5-вариантов покрытие держится ~100 % до порога 0.85 — шкала другая, `GAMMA_LINK_MIN_SCORE`
перекалибровывать под кривую.

Дополнение 2026-09-24 — контрольный прогон на публичном корпусе (по просьбе владельца):
2100 документов с метками из трёх публичных HF-датасетов (mteb/mlsum ru, SetFit/bbc-news,
mteb/GeoreviewClusteringP2P; сборщик `nlp-service/scripts/fetch_public_corpus.py`, seed 20260924,
67 папок). Тот же скрипт и те же 8 вариантов. Числа — в
[`MODEL-1B-public-findings.md`](MODEL-1B-public-findings.md); локальные артефакты —
`work-model1b/local/public/`.

Ключевое отличие результата: на публичном корпусе бутстрэп F − E = [0.0083; 0.0276] — ноль
снаружи, по правилу выходит **e5-base** (на приватном интервал включал ноль → e5-small).
Корпуса меряют разное: приватный — реальные заметки с мусором импорта, публичный — чистые
тексты, 1400/2100 без заголовков. Совет правила по двум корпусам разошёлся — решение
модели остаётся за владельцем.

Проверки повторены: приватность (0 совпадений по обоим корпусам), pytest nlp-service 64 зелёных,
прод не тронут (diff — только скрипты замера и доки), индекс задач/доска/ссылки зелёные.
