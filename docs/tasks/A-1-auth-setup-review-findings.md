# Setup авторизации для визуального эталона. Замечания ревью

Ревьюер: Claude Code, 2026-09-07. Исполнитель: Devin. Предмет: `c88d65d` (`frontend/tests/setup/auth.setup.ts`, проект `visual-real-auth` в `playwright.config.ts`) и отчёт `c3dd1cf`.

Статус: **не принято.** Setup написан аккуратно и делает ровно то, что заявлено, но цели не достигает: авторизованный эталон снимет анонимный публичный вид — то самое, ради ухода от чего он и заводился.

## Что сделано правильно

| Что | Проверка |
|---|---|
| Вход выполняется и состояние сохраняется | `npx playwright test --project=setup-auth` → `1 passed (5.1s)` |
| В состоянии есть обе HttpOnly-куки | `access_token@127.0.0.1`, `refresh_token@127.0.0.1` |
| В состоянии есть подсказка сессии | `localStorage: kg_auth_session, cockpit-settings` |
| Проекты разведены | `setup-skip` и `setup-auth` вместо общего `setup`, `visual-real-auth` зависит от второго |
| Файл состояния не попадёт в git | добавлен в `frontend/.gitignore` |

Претензий к самому setup нет: он сохраняет ровно то, что нужно для восстановления сессии.

## Блокер: приложение стирает подсказку сессии до того, как ею воспользуется

Проба с перехватом `localStorage.removeItem` на старте документа:

```json
{"hintAtStart":"1","hintNow":null,"removed":["api_key","kg_auth_session"]}
```

Читается так: в момент старта подсказка **на месте** — значит Playwright применил состояние верно. Затем инициализация приложения сама вызывает `removeItem("api_key")` и `removeItem("kg_auth_session")`, и подсказка исчезает.

Дальше срабатывает ветка `auth.svelte.ts:145-149`: нет токена в памяти, нет API-ключа, нет подсказки — «браузер анонимен», выходим не обращаясь к `/v1/auth/refresh`. Куки остаются валидными, но ими никто не пользуется.

Наблюдаемое следствие — страница с сохранённой сессией ведёт себя как анонимная:

```
1500ms  {"nodes":20,"links":60,"signIn":true}
4000ms  {"nodes":20,"links":60,"signIn":true}
8000ms  {"nodes":20,"links":60,"signIn":true}
15000ms {"nodes":20,"links":60,"signIn":true}
вызовы: ["/graph-service/api/v1/graph/public?limit=100"]
```

Пятнадцать секунд, надпись «Sign in» на месте, единственный запрос — публичный граф. Ни `/auth/refresh`, ни `/users/me` не вызываются вовсе.

Для сравнения, приватный граф на этих же данных дал бы 100 узлов вместо 20: в базе 100 заметок, публичных из них 20.

**Почему это блокер.** Эталон, снятый проектом `visual-real-auth`, будет неотличим от анонимного: те же 20 узлов, та же кнопка «Sign in». Второй эталон не добавит покрытия, зато создаст впечатление, что авторизованный вид проверяется.

## Что выяснить и починить

Найти, кто вызывает `removeItem("kg_auth_session")` при инициализации, и почему это происходит **до** попытки восстановления. По вызовам видно, что очистка идёт без обращения к `/auth/refresh`, то есть это не реакция на неудачный рефреш, а безусловная очистка на старте.

Целевое поведение: при наличии подсказки и валидной refresh-куки приложение обязано попытаться восстановить сессию, и только при неудаче — чистить. Проверяемый признак успеха — вызов `v1/graph/full` вместо `v1/graph/public` и отсутствие «Sign in».

## Мелочь: относительный путь к состоянию

`auth.setup.ts:8` и `playwright.config.ts` задают `tests/setup/.auth/testuser.json` относительным путём. Playwright разрешает его от текущего каталога процесса, а не от каталога конфигурации.

Запуск из корня репозитория создаёт `D:\knowledge-graph\tests\setup\.auth\testuser.json` — вне `frontend/`, вне его `.gitignore`, и проект `visual-real-auth` этого файла не увидит. Проверено: файл там появился, я его удалил.

Штатные npm-скрипты запускаются из `frontend/`, поэтому в обычной работе всё сходится. Но ловушка тихая: я на ней задержался, приняв устаревший файл за свежий и едва не сделав неверный вывод. Лечится `path.resolve(__dirname, ...)` или добавлением в `.gitignore` корня.

## Про мою же проверку

Первый прогон я запустил из корня репозитория, и он записал состояние не туда. Проба при этом читала файл от 6 сентября и показывала тот же результат — совпадение, которое могло увести в сторону. Вывод сделан только после повторного прогона из `frontend/` со свежим состоянием; оба прогона дали одинаковую картину, поэтому находка устояла.
