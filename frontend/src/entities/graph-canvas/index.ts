/**
 * Graph canvas domain helpers and types.
 * Previously located under components/organisms/GraphCanvas; moved here because
 * they depend on domain entities and are consumed by both widgets and features.
 */

export * from "./lib";
