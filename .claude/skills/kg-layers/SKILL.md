---
name: kg-layers
description: Границы слоёв — Clean Architecture на бэкенде и FSD на фронтенде: что кому можно импортировать и как проверить.
---

Применяется при добавлении импортов, переносе файлов между слоями и ревью изменений в `backend/internal/` или `frontend/src/`.

Канонический текст — [`.devin/skills/kg-layers/SKILL.md`](../../../.devin/skills/kg-layers/SKILL.md). Прочитай его целиком, прежде чем действовать: содержимое хранится в одном экземпляре, чтобы версии для Claude Code и Devin не разошлись.
